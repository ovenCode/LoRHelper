<UserControl
    x:Class="desktop.pages.InGamePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:desktop="clr-namespace:desktop"
    xmlns:pages="clr-namespace:desktop.pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:desktop.ViewModels"
    d:DataContext="{d:DesignInstance Type=vm:InGameViewModel}"
    mc:Ignorable="d"
    d:DesignHeight="800"
    d:DesignWidth="400">

    <Grid Background="{StaticResource BackgroundColor}" Width="300">        
        <Button x:Name="backBtn" Height="40" Width="40" Command="{Binding GoBackCommand}" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10">
            <Image Source="../assets/icons/icon-back.png" />
        </Button>
        <StackPanel Orientation="Horizontal" Height="30" Margin="0,80,0,0" VerticalAlignment="Top" HorizontalAlignment="Center">
            <TextBox x:Name="searchBoxTB" Style="{StaticResource SearchBox}" Height="30" Width="200" VerticalContentAlignment="Center" VerticalAlignment="Top" Margin="0,0,10,0"/>
            <Button x:Name="addNewBtn" Height="30" Command="{Binding AddNewCommand}">
                <Image Source="../assets/icons/icon-add.png" />
            </Button>
        </StackPanel>
        <ScrollViewer x:Name="inGameSV" VerticalAlignment="Top" Margin="0,120,0,0" VerticalScrollBarVisibility="Auto" >
            <StackPanel>
                <Label x:Name="encounterLbl"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <Label x:Name="gameStateLbl" Content="Stan gry" Margin="0,0,80,0"/>
                    <Button x:Name="endGameBtn" Content="Zakończ grę" HorizontalAlignment="Right"/>
                </StackPanel>
                <StackPanel>
                <Label x:Name="strongestLbl" Content="Najsilniejsza jednostka" Margin="0,0,0,10"/>
                </StackPanel>
                <Label Content="Talia" Margin="0,0,0,10"/>
                <Label Content="Filtry:" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <ToggleButton Style="{StaticResource FilterToggleBtn}" x:Name="drawnCardsBtn" Content="Dobrane" />
                    <ToggleButton Style="{StaticResource FilterToggleBtn}" x:Name="remainingCardsBtn" Content="Pozostałe" Margin="5,0,0,0" />
                </StackPanel>
                <ScrollViewer x:Name="deckSV" Height="150" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                    <ListBox x:Name="cardsLB" Background="Transparent" BorderBrush="Transparent" ItemsSource="{Binding Deck}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <ListBoxItem Style="{StaticResource CardItem}"/>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        <!--<ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <Label Content="Test" />
                                </StackPanel>
                            </DataTemplate>
                            --><!-- <local:ListBoxItemCard /> --><!--
                        </ListBox.ItemTemplate>-->
                        <!-- <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Koszt"/>
                            <TextBlock Margin="10,0" Text="Nazwa"/>
                            <TextBlock Text="Szansa na dobranie"/>
                            </StackPanel> -->
                        
                    </ListBox>                    
                </ScrollViewer>
                <Button x:Name="loadCardsBtn" Content="Załaduj karty" Width="120" Margin="0,5,0,0"/>
                <Label Content="Wszystkie karty" x:Name="allCardsLBL" />
                <StackPanel x:Name="allCardSP">
                    <TextBox Style="{StaticResource SearchBox}" x:Name="FilterCards" Width="200" Height="25"  />
                    <ScrollViewer x:Name="allCardsSV" Height="200" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                        <ListBox x:Name="allCardsLB" Background="Transparent" BorderBrush="Transparent" ItemsSource="{Binding PlayedCards}">
                        </ListBox>
                    </ScrollViewer>
                </StackPanel>
                <Label Content="Karty dobrane przez przeciwnika"/>
                <Label Content="Filtry"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <ToggleButton Style="{StaticResource FilterToggleBtn}" x:Name="spellCardsBtn" Content="Zaklęcia" />
                    <ToggleButton Style="{StaticResource FilterToggleBtn}" x:Name="unitCardsBtn" Content="Jednostki" Margin="5,0,0,0" />
                    <ToggleButton Style="{StaticResource FilterToggleBtn}" x:Name="locationCardsBtn" Content="Lokacje" Margin="5,0,0,0" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
        <Grid x:Name="cardPreviewGrid" Background="#aa000000" Visibility="Hidden">
            <StackPanel x:Name="cardPreview" HorizontalAlignment="Center" VerticalAlignment="Center"  >
                <!--<Border VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Image x:Name="cardInspect" />
                </Border>
                <ScrollViewer>
                    <StackPanel x:Name="cardInfo"/>
                </ScrollViewer>-->
                <!-- <ScrollViewer>
                    <StackPanel x:Name="cardInfo"/>
                </ScrollViewer> -->
                <desktop:CardPreview DataContext="{Binding CardPreview}" />
            </StackPanel>
        </Grid>
        <Grid x:Name="addDialogGrid" VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="{Binding ShowPopUp}">
            <Border Background="#2F2A30" BorderBrush="#2F2A30" CornerRadius="4">
                <StackPanel Margin="40">
                    <Button x:Name="addPowersButton" Content="Dodaj moce" Command="{Binding AddPowersCommand}" Background="#FF8B5911" Margin="0,0,0,15"/>
                    <Button x:Name="addMissionsBtn" Content="Dodaj misje" Command="{Binding AddMissionsCommand}" Background="#FF8B5911" Margin="0,0,0,15"/>
                    <Button x:Name="closeAddDialogBtn" Content="Zamknij" Background="#FF8B5911" Margin="0,0,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>